if [ "X"${1} == Xall ]
then
    dockdev down app-db crmupload-web crmupload-worker-upload crmupload-worker-duplicate-check crmupload-worker-duplicate-check-gpu-dev crmupload-worker-duplicate-check-gpu-prod crmupload-mailpit
else
    dockdev down app-db crmupload-web crmupload-worker-upload crmupload-worker-duplicate-check crmupload-worker-duplicate-check-gpu-dev crmupload-mailpit
fi
