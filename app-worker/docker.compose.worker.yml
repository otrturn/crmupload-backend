services:
  crmupload-worker:
    image: crmupload-worker
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crmupload-worker
    restart: unless-stopped
    networks: [ crmuploadnet ]
    volumes:
      - /home/ralf/tmp:/data/tmp
    extra_hosts:
      - "host.docker.internal:host-gateway"
networks:
  crmuploadnet:
    external: true